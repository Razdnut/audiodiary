pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
    mavenCentral()
  }
}

dependencyResolutionManagement {
  repositories {
    google()
    mavenCentral()
  }
}

rootProject.name = "audiodiary"

include(":app")
include(":capacitor-cordova-android-plugins")

apply from: file('capacitor.settings.gradle')
